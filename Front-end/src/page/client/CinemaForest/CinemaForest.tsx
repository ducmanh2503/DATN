import React from "react";
import Layout from "../Layout";

const CinemaForest = () => {
    return (
        <div>
            <Layout></Layout>
        </div>
    );
};

export default CinemaForest;
